/**
 * The Linux-AT & T assembly format.
 */

/**
 * The registries used for function arguments.
 */
char* ATTLINUX_ARGUMENT_REGISTRIES[] = {"rdi", "rsi", "rdx", "rcx", "r8", "r9"};

/**
 * The types of different data values that are registered in sections and not the stack.
 */
char* ATTLINUX_SECTION_TYPES[] = {".string"};
