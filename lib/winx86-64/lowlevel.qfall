/**
 * The quickfall std::lowlevel for Windows x86-64 architecture.
 */

/**
 * Allocates some memory on the stack
 */ 
func salloc(int size) {}

/**
 * Marks a portion of the stack of the provided size to be unused.
 */
func sfree(int size) {}

/**
 * Puts a value of the provided type into a stack section.
 */
func stackPut(? type, ? value) {}